{
  "name": "indeo-institute-site",
  "private": true,
  "type": "module",
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/identity": "^2.0.0"
  },
  "scripts": {
    "build": "pagefind --site public --output-subdir _pagefind --glob \"**/*.html\" --exclude-selectors \"nav,footer\""
  },
  "devDependencies": {
    "netlify-cli": "^17.38.0",
    "pagefind": "^1.3.0"
  }
}
